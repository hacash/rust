
pub const ACTION_LV_TOP_ONLY: u8 = 0; // only this one on top
pub const ACTION_LV_TOP_UNIQUE: u8 = 1; // top and single unique
pub const ACTION_LV_TOP: u8 = 2; // must on top
pub const ACTION_LV_TX_ACT: u8 = 3; // must in tx action list (can in if vmaction)

